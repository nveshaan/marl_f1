defaults:
  - cnn

name: ctde_cnn
policy: ${algo.model.policy}
policy_kwargs:
  features_extractor_class: 
    _target_: models.CTDEFeaturesExtractor
    _partial_: true
  features_extractor_kwargs:
    n_agents: ${task.env_kwargs.num_agents}